print(nv)
Tcorv<-cor(Yv,Xv,method="spearman")
A<-length(Tcorv)
}
C4<-nv/n
Yv<-Y
Xv<-TCov
nv<-nrow(Xv)
A<-1
Tcorv<-cor(Yv,Xv,method="spearman")
#while((A != 0) & (nv != 0)){
while((A != 0) & (nv >9)){
mv<-which(abs(Tcorv)==max(abs(Tcorv),na.rm=TRUE))[1]
regmv<-lm(Yv~Xv[,mv])
residmv<-residuals(regmv)
indexresid<-which(abs(residmv)<0.01)
print(length(indexresid))
Yv<-Yv[-indexresid]
Xv<-Xv[-indexresid,-mv]
nv<-nrow(Xv)
print(nv)
Tcorv<-cor(Yv,Xv,method="spearman")
A<-length(Tcorv)
}
C4<-nv/n
which(abs(residmv)<0.01)
indexresid
Xv[-indexresid,-mv]
Xv[-indexresid,-mv]
dim(Xv)
length(indexresid)
Yv<-Y
Xv<-TCov
nv<-nrow(Xv)
A<-1
Tcorv<-cor(Yv,Xv,method="spearman")
#while((A != 0) & (nv != 0)){
while((A != 0) & (nv >9)){
mv<-which(abs(Tcorv)==max(abs(Tcorv),na.rm=TRUE))[1]
regmv<-lm(Yv~Xv[,mv])
residmv<-residuals(regmv)
indexresid<-which(abs(residmv)<0.01)
print(length(indexresid))
if(length(indexresid)==0){
break
}
Yv<-Yv[-indexresid]
Xv<-Xv[-indexresid,-mv]
nv<-nrow(Xv)
print(nv)
Tcorv<-cor(Yv,Xv,method="spearman")
A<-length(Tcorv)
}
C4<-nv/n
C4
1032/2000
Yv<-Y
Xv<-TCov
nv<-nrow(Xv)
A<-1
Tcorv<-cor(Yv,Xv,method="spearman")
#while((A != 0) & (nv != 0)){
while((A != 0) & (nv >9)){
mv<-which(abs(Tcorv)==max(abs(Tcorv),na.rm=TRUE))[1]
regmv<-lm(Yv~Xv[,mv])
residmv<-residuals(regmv)
indexresid<-which(abs(residmv)<0.05)
print(length(indexresid))
if(length(indexresid)==0){
break
}
Yv<-Yv[-indexresid]
Xv<-Xv[-indexresid,-mv]
nv<-nrow(Xv)
print(nv)
Tcorv<-cor(Yv,Xv,method="spearman")
A<-length(Tcorv)
}
C4<-nv/n
C4
Yv<-Y
Xv<-TCov
nv<-nrow(Xv)
A<-1
Tcorv<-cor(Yv,Xv,method="spearman")
#while((A != 0) & (nv != 0)){
while((A != 0) & (nv >9)){
mv<-which(abs(Tcorv)==max(abs(Tcorv),na.rm=TRUE))[1]
regmv<-lm(Yv~Xv[,mv])
residmv<-residuals(regmv)
indexresid<-which(abs(residmv)<0.1)
print(length(indexresid))
if(length(indexresid)==0){
break
}
Yv<-Yv[-indexresid]
Xv<-Xv[-indexresid,-mv]
nv<-nrow(Xv)
print(nv)
Tcorv<-cor(Yv,Xv,method="spearman")
A<-length(Tcorv)
}
C4<-nv/n
c4
C4
regL1<-lm(Y~TCov)
regL1<-lm(Y~.,data=TCov)
summary(regL1)
residL1<-residuals(regmv)
summary(residL1)
length(residL1)
residL1
TData_unnorm <- read.csv(file="CHDScenario16DS1.csv", header=TRUE)
# Check if variable is binary
checkNonBinaryTrait = function(v, naVal="NA") {
ind<-0
if(!is.numeric(v)) stop("Only numeric vectors are accepted.")
vSet = unique(v)
if(!missing(naVal)) vSet[which(vSet == naVal)] = NA
vSet = vSet[which(!is.na(vSet))]
if(any(as.integer(vSet) != vSet)) ind<-1
if(length(vSet) > 2) ind<-1
return (ind)
}
nonbinInd<-apply(TData_unnorm,2, checkNonBinaryTrait)
# Number of variables (including treatment and outcome)
nvar<-ncol(TData_unnorm)
# normalize all non-binary data to 0-1 (we suppose that all binary variables are coded 0-1)
for(j in 1:nvar){
if(nonbinInd[j]==1){
TData_unnorm[,j]<-(TData_unnorm[,j]-min(TData_unnorm[,j]))/(max(TData_unnorm[,j])-min(TData_unnorm[,j]))
}
}
TData<-TData_unnorm
# nb observations
n<-nrow(TData)
# Get covariate only
TCov<-TData[,-(1:2)]
# nb covariates
d<-ncol(TCov)
# Get outcome only
Y<-TData[,1]
dim(TCov)
length(Y)
regL1<-lm(Y~.,data=TCov)
residL1<-residuals(regmv)
L1<-mean(abs(residL1))
summary(regL1)
summary(residL1)
length(residL1)
residuals(regL1)
residL1<-residuals(regL1)
length(residL1)
L1<-mean(abs(residL1))
l1
L1
regL<-lm(Y~.,data=TCov)
residL<-residuals(regL)
L1<-mean(abs(residL))
L2<-mean(residL^2)
L2
require(stats)
d <- dist(TCov)
dim(d)
d
require(emstreeR)
install.packages("emstreeR")
mlpack_mst(TCov)
require(emstreeR)
mlpack_mst(TCov)
covMST<-ComputeMST(TCov)
dim(covMST)
covMST[1:10,]
sum(covMST[1,1:200])
set.seed(1984)
n <- 15
c1 <- data.frame(x = rnorm(n, -0.2, sd = 0.2), y = rnorm(n, -2, sd = 0.2))
c2 <- data.frame(x = rnorm(n, -1.1, sd = 0.15), y = rnorm(n, -2, sd = 0.3))
d <- rbind(c1, c2)
d <- as.data.frame(d)
d
out <- ComputeMST(d)
out
plot(out)
covMST<-ComputeMST(TCov)
covMST
covMST<-ComputeMST(TCov)
indMST1<-covMST[,d+1]
indMST2<-covMST[,d+2]
YMST1<-Y[indMST1]
YMST2<-Y[indMST2]
YMST<-abs(YMST1-YMST2)
S1<-mean(YMST)
d
# nb covariates
d<-ncol(TCov)
indMST1<-covMST[,d+1]
indMST2<-covMST[,d+2]
YMST1<-Y[indMST1]
YMST2<-Y[indMST2]
YMST<-abs(YMST1-YMST2)
S1<-mean(YMST)
YMST
length(YMST)
YMST[2000]
cbind(YMST1,YMST2)
Eucd<-dist(TCov)
dim(Eucd)
Eucd[1:10,1:10]
length(Eucd)
is.matrix(TCov)
Eucd<-dist(as.matrix(TCov))
dim(Eucd)
Eucd<-as.dist(as.matrix(TCov),diag = FALSE, upper = FALSE)
Eucd <- as.matrix(dist(as.matrix(TCov)))
is.matrix(Eucd)
dim(Eucd)
Eucd[1:10,]
x <- matrix(rnorm(100), nrow = 5)
dist(x)
dist(x, diag = TRUE)
dist(x, upper = TRUE)
m <- as.matrix(dist(x))
Eucd <- as.matrix(dist(as.matrix(TCov)),diag = TRUE)
Eucd <- as.matrix(dist(as.matrix(TCov)),diag = TRUE,upper = TRUE)
Eucd
Eucd[1,2]
Eucd[1,1]
Eucd[2,1]
Eucd[1,5]
Eucd[5,1]
dist(TCov[1,],TCov[5,])
TCov[1,]
TCov[5,]
sqrt(sum((TCov[1,]-TCov[5,])^2)))
sqrt(sum((TCov[1,]-TCov[5,])^2))
ranky<-rank(Y,ties.method = "random")
ranky
Y[1781]
sorty<-sort(Y, index.return=TRUE)
sorty
indsorty<-sort(Y, index.return=TRUE)$ix
indsorty<-sort(Y, index.return=TRUE)$ix
indsorty
suminput<-0
i=2
indsorty[i]
indsorty[i-1]
indsorty[i]
Eucd[indsorty[i-1],indsorty[i]]
indsorty[1501:2000]
i=n
indsorty[i-1]
n
n=2000
indsorty[i-1]
i
i=n
indsorty[i-1]
indsorty[i]
# Distance matrix between observation
Eucd <- as.matrix(dist(as.matrix(TCov)),diag = TRUE,upper = TRUE)
# get indices for sorted Y
indsorty<-sort(Y, index.return=TRUE)$ix
suminput<-0
for(i in 2:n){
suminput<-suminput+Eucd[indsorty[i-1],indsorty[i]]
}
S2<-suminput/n
S2
# get indices for sorted Y
indsorty<-sort(Y, index.return=TRUE)$ix
suminput<-0
for(i in 2:n){
print(suminput)
suminput<-suminput+Eucd[indsorty[i-1],indsorty[i]]
}
S2<-suminput/n
i=1
which.min(Eucd[i,])
Eucdwodiag <- as.matrix(dist(as.matrix(TCov)),upper = TRUE)
which.min(Eucdwodiag[i,])
Eucdwodiag[1,1]
i
Eucdi<-Eucdw[i,]
Eucdi<-Eucd[i,]
iNN<-which((Eucdi!=0)&(Eucdi==max(Eucdi)))
iNN
Eucd[1,1031]
Eucd[1,1032]
which((Eucdi!=0)&(Eucdi==min(Eucdi)))
Eucdi<-Eucd[i,]
Eucdi[i]<-Inf
iNN<-which.min(Eucdi)
iNN
Eucd[1,472]
Eucd[1,470]
Eucd[1,970]
Eucd[1,1970]
i=2
Eucd[i,]
Eucdi[i]<-Inf
Eucdi
i=1
Eucdi<-Eucd[i,]
Eucdi
Eucdi[i]<-Inf
Eucdi[i]<-Inf
Eucdi
i=2
Eucdi<-Eucd[i,]
Eucdi
Eucdi[i]<-Inf
Eucdi
ErrorS3<-0
for(i in 1:n){
Eucdi<-Eucd[i,]
Eucdi[i]<-Inf
iNN<-which.min(Eucdi)
ErrorS3<-ErrorS3+(Y[i]-Y[iNN])^2
}
S3<-ErrorS3/n
ErrorS3<-0
for(i in 1:n){
Eucdi<-Eucd[i,]
Eucdi[i]<-Inf
iNN<-which.min(Eucdi)
print(ErrorS3)
ErrorS3<-ErrorS3+(Y[i]-Y[iNN])^2
}
S3<-ErrorS3/n
x <- c(2,2:4,4,4,5,5,7,7,7)
y <- c(1:6, 5:4, 3:1)
x\
x
y
amy <- approx(x, y, xout = x)$y)
amy <- approx(x, y, xout = x)$y
amy
sorty
OrderY<-Y[indsorty]
OrderY
OrderY<-Y[indsorty]
OrderX<-TCov[indsorty,]
i=1
Xprime<-runif(d,min=OrderX[i,],max=OrderX[i+1,])
runif(2,c(3,4),c(2,5))
runif(1,5,1)
runif(1,1,5)
OrderY<-Y[indsorty]
OrderX<-TCov[indsorty,]
i=1
MSE<-0
l<-0
while(i<n-1){
Xprime<-rep(0,d)
for(j in 1:d){
minxrunif<-min(OrderX[i,d],OrderX[i+1,d])
maxxrunif<-max(OrderX[i,d],OrderX[i+1,d])
Xprime[d]<-runif(1,minxrunif,maxxrunif)
}
minyrunif<-min(OrderY[i],OrderY[i+1])
maxyrunif<-max(OrderY[i],OrderY[i+1])
Yprime<-runif(1,minyrunif,maxyrunif)
MSE<-MSE+(predict.lm(regL,Xprime)-Yprime)^2
i=i+1
l<-l+1
}
L3<-MSE/l
predict.lm(regL,Xprime)
predict.lm()
predict.lm
predict.lm(regL,TCov[2,]
)
OrderY<-Y[indsorty]
OrderX<-TCov[indsorty,]
i=1
MSE<-0
l<-0
while(i<n-1){
Xprime<-rep(0,d)
for(j in 1:d){
minxrunif<-min(OrderX[i,d],OrderX[i+1,d])
maxxrunif<-max(OrderX[i,d],OrderX[i+1,d])
Xprime[j]<-runif(1,minxrunif,maxxrunif)
}
minyrunif<-min(OrderY[i],OrderY[i+1])
maxyrunif<-max(OrderY[i],OrderY[i+1])
Yprime<-runif(1,minyrunif,maxyrunif)
MSE<-MSE+(predict.lm(regL,Xprime)-Yprime)^2
i=i+1
l<-l+1
}
L3<-MSE/l
Xprime
i=1
MSE<-0
l<-0
while(i<n-1){
Xprime<-rep(0,d)
for(j in 1:d){
minxrunif<-min(OrderX[i,j],OrderX[i+1,j])
maxxrunif<-max(OrderX[i,j],OrderX[i+1,j])
Xprime[j]<-runif(1,minxrunif,maxxrunif)
}
minyrunif<-min(OrderY[i],OrderY[i+1])
maxyrunif<-max(OrderY[i],OrderY[i+1])
Yprime<-runif(1,minyrunif,maxyrunif)
MSE<-MSE+(predict.lm(regL,Xprime)-Yprime)^2
i=i+1
l<-l+1
}
L3<-MSE/l
Xprime
predict.lm(regL,Xprime)
predict.lm(regL,Xprime)
regLcoef<-regL$coefficients
regLcoef
predXprime<-regLcoef%*%c(1,Xprime)
predXprime
#regL<-lm(Y~.,data=TCov)
regLcoef<-regL$coefficients
#indsorty<-sort(Y, index.return=TRUE)$ix
OrderY<-Y[indsorty]
OrderX<-TCov[indsorty,]
i=1
MSE<-0
l<-0
while(i<n-1){
Xprime<-rep(0,d)
for(j in 1:d){
minxrunif<-min(OrderX[i,j],OrderX[i+1,j])
maxxrunif<-max(OrderX[i,j],OrderX[i+1,j])
Xprime[j]<-runif(1,minxrunif,maxxrunif)
}
predXprime<-regLcoef%*%c(1,Xprime)
minyrunif<-min(OrderY[i],OrderY[i+1])
maxyrunif<-max(OrderY[i],OrderY[i+1])
Yprime<-runif(1,minyrunif,maxyrunif)
MSE<-MSE+(predXprime-Yprime)^2
print(MSE)
i=i+1
l<-l+1
print(l)
}
L3<-MSE/l
OrderY<-Y[indsorty]
OrderX<-TCov[indsorty,]
i=1
Xprime<-matrix(0,nrow=n-1,ncol=d)
Yprime<-rep(0,n-1)
while(i<n-1){
for(j in 1:d){
minxrunif<-min(OrderX[i,j],OrderX[i+1,j])
maxxrunif<-max(OrderX[i,j],OrderX[i+1,j])
Xprime[i,j]<-runif(1,minxrunif,maxxrunif)
}
minyrunif<-min(OrderY[i],OrderY[i+1])
maxyrunif<-max(OrderY[i],OrderY[i+1])
Yprime[i]<-runif(1,minyrunif,maxyrunif)
i=i+1
#print(i)
}
predXprime<-knn.reg(OrderX, test = Xprime, OrderY, k = 1, algorithm="kd_tree")
S4<-(sum(predXprime-Yprime)^2)/n-1
install.packages("FNN")
require(FNN)
predXprime<-knn.reg(OrderX, test = Xprime, OrderY, k = 1, algorithm="kd_tree")
predXprime
predXprime2<-knn.reg(OrderX, test = Xprime, OrderY, k = 1, algorithm="brute")
sum(predXprime==predXprime2)
S4<-sum((predXprime-Yprime)^2)/n-1
predXprime<-as.vector(knn.reg(OrderX, test = Xprime, OrderY, k = 1, algorithm="kd_tree"))
predXprime
predXprime<-knn.reg(OrderX, test = Xprime, OrderY, k = 1, algorithm="kd_tree")$pred
predXprime
predXprime2<-knn.reg(OrderX, test = Xprime, OrderY, k = 1, algorithm="brute")$pred
sum(predXprime==predXprime2)
predXprime<-knn.reg(OrderX, test = Xprime, OrderY, k = 2, algorithm="kd_tree")$pred
predXprime2<-knn.reg(OrderX, test = Xprime, OrderY, k = 2, algorithm="brute")$pred
sum(predXprime==predXprime2)
predXprime<-knn.reg(OrderX, test = Xprime, OrderY, k = 5, algorithm="kd_tree")$pred
predXprime2<-knn.reg(OrderX, test = Xprime, OrderY, k = 5, algorithm="brute")$pred
sum(predXprime==predXprime2)
predXprime3<-knn.reg(OrderX, test = Xprime, OrderY, k = 5, algorithm="cover_tree")$pred
sum(predXprime==predXprime3)
S4<-sum((predXprime-Yprime)^2)/n-1
S4
S4<-sum((predXprime-Yprime)^2)/n-1
(predXprime-Yprime)^2
S4<-sum((predXprime-Yprime)^2)/(n-1)
S4
OrderY<-Y[indsorty]
OrderX<-TCov[indsorty,]
i=1
Xprime<-matrix(0,nrow=n-1,ncol=d)
Yprime<-rep(0,n-1)
while(i<n-1){
for(j in 1:d){
minxrunif<-min(OrderX[i,j],OrderX[i+1,j])
maxxrunif<-max(OrderX[i,j],OrderX[i+1,j])
Xprime[i,j]<-runif(1,minxrunif,maxxrunif)
}
minyrunif<-min(OrderY[i],OrderY[i+1])
maxyrunif<-max(OrderY[i],OrderY[i+1])
Yprime[i]<-runif(1,minyrunif,maxyrunif)
i=i+1
#print(i)
}
predXprime<-knn.reg(OrderX, test = Xprime, OrderY, k = 1, algorithm="kd_tree")$pred
S4<-sum((predXprime-Yprime)^2)/(n-1)
S4
